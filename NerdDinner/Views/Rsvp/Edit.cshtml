@model NerdDinner.Models.Rsvp

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rsvp</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RsvpId" />
            <div class="form-group">
                <label asp-for="DinnerId" class="control-label"></label>
                <select asp-for="DinnerId" class="form-control" asp-items="ViewBag.DinnerId"></select>
                <span asp-validation-for="DinnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                <input asp-for="UserName" class="form-control" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
